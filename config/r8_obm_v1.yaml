strategy_id: R8_OBM_V1
strategy_version: "1.00"
status: FORWARD_TEST_SIGNAL_ONLY
source: "External MQL5 EA, discovered running in MT5 terminal 2026-08-25, registered same day"

# This is NOT a Python component of this repo. It documents and tracks the governance state
# of an external MetaTrader 5 Expert Advisor that lives in the MT5 terminal's own data folder,
# not in this git repository. See R8_OBM_V1_SPEC.md for full detail.
engine:
  type: MQL5_EXPERT_ADVISOR
  location: "%APPDATA%\\MetaQuotes\\Terminal\\8EFB2DF501EAEF188AB46828829DBF78\\MQL5\\Experts\\R8_OBM_V1_EA (1).mq5"
  compiled: "%APPDATA%\\MetaQuotes\\Terminal\\8EFB2DF501EAEF188AB46828829DBF78\\MQL5\\Experts\\R8_OBM_V1_EA (1).ex5"
  journal: "%APPDATA%\\MetaQuotes\\Terminal\\8EFB2DF501EAEF188AB46828829DBF78\\MQL5\\Files\\R8_OBM_V1_EURUSD_journal.csv"
  tracked_in_this_repo: false
  # 2026-08-25: a duplicate "R8_OBM_V1_EA.mq5"/".ex5" (no "(1)" suffix) existed alongside this
  # one, unattached and unused. Deleted for clarity - only the "(1)" copy, which is the one
  # actually attached to the EURUSD M15 chart, now exists.

strategy:
  symbol: EURUSD
  range_pips: 8.0
  sl_buffer_pips: 1.0
  reward_risk: 1.5
  one_position_at_a_time: true
  max_spread_pips: 2.0
  magic_number: 8101501

execution_gates:
  require_demo_account: true
  real_account_block: unconditional_hardcoded
  allow_demo_trading_default: false
  allow_demo_trading_current: false  # confirmed 2026-08-25 via chart panel + terminal log

related_lineage:
  backtest_reference: "ST-01 RANGE8 MOMENTUM (external research notes, not in this repo) - REJECT V1, bid/ask PF 0.81"
  python_sibling: "mt5_range_bar_live.py (magic 108801, different implementation, also inert)"
  relationship: "Same underlying idea (range-bar one-bar momentum), independent implementations, no shared code, no magic-number collision"

governance:
  baseline_mutation_allowed: false
  promotion_allowed_from_this_sample: false
  live_execution_authorized: false
  demo_execution_authorized: false  # signal-only until explicitly reviewed and flipped by the trader
  note: >
    2026-08-25 validation found the terminal briefly attached to a REAL account earlier the same
    day (MQL5/logs/20260825.log, 19:00:27, "Account mode: REAL") while this EA was loaded. The
    EA's hard-coded real-account block correctly prevented any order attempt. See spec section 6.
